{{! Sheet Header }}
<div>
  {{#if (eq item.type 'trait')}}
  {{> ($$ "item/trait/header")}}
  {{/if}}

  {{#if (eq item.type 'gear')}}
  {{> ($$ "item/gear/header")}}
  {{/if}}

  {{#if (eq item.type 'burden')}}
  {{> ($$ "item/burden/header")}}
  {{/if}}
</div>
