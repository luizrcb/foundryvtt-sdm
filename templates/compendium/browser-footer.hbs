<div class="summary">
  {{#if displaySelection}}
    <span>{{summary}}</span>
    {{#if invalid}}<i class="fas fa-exclamation-triangle warning" data-tooltip="{{invalidTooltip}}"></i>{{/if}}
  {{/if}}
</div>